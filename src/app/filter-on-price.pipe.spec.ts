import { FilterOnPricePipe } from './filter-on-price.pipe';

describe('FilterOnPricePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOnPricePipe();
    expect(pipe).toBeTruthy();
  });
});
